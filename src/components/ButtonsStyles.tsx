import { StyleSheet } from "react-native";

export const buttonsStyles = StyleSheet.create({
  button: {
    width: "100%",
    padding: 16,
    borderRadius: 12,
    alignItems: "center",
    borderWidth: 1,
  },
  buttonText: {
    fontSize: 16,
    fontWeight: "600",
  },
});
